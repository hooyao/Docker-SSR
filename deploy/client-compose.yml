version: '2'
services:
  ss-client:
    container_name: ss-client
    image: image: hooyao/ss-libev:ss
    volumes:
      - ./client.json:/root/config/config.json
    entrypoint: ss-local
    command: -c /root/config/config.json
    ports:
      - "1080:1080"
      - "1080:1080/udp"